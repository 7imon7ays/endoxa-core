var EndoxaCore = require('../module'),
  chai       = require('chai'),
  expect     = chai.expect;

describe('EndoxaCore', function() {
  it('exists', function() {
    expect(EndoxaCore).to.be.an('object');
  });
});
